CREATE TABLE investments (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    symbol TEXT NOT NULL,
    shares INTEGER NOT NULL,
    buy_date TIMESTAMPTZ DEFAULT now() NOT NULL,
    value_at_buy_date INTEGER NOT NULL
);
